import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.karangkraf.app',
  appName: 'KarangkrafApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
